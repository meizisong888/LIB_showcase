import type { SkillFilters } from '../lib/filters'
import { tools } from '../data/tools'

export function AdvancedFilters({ filters, update }: { filters: SkillFilters; update: (key: keyof SkillFilters, value: string) => void }) {
  return <details className="advanced-filters"><summary>Advanced filters</summary><div className="advanced-filter-grid"><label><span>Compatible product</span><select value={filters.tool} onChange={(event) => update('tool', event.target.value)}><option value="">Any product</option>{tools.map((tool) => <option key={tool.id} value={tool.id}>{tool.name}</option>)}</select></label><label><span>Input type</span><select value={filters.inputType} onChange={(event) => update('inputType', event.target.value)}><option value="">Any input</option>{['document', 'PDF', 'web sources', 'repository', 'spreadsheet', 'notes'].map((item) => <option key={item}>{item}</option>)}</select></label><label><span>Risk level</span><select value={filters.risk} onChange={(event) => update('risk', event.target.value)}><option value="">Any risk</option><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option></select></label><label><span>Internet access</span><select value={filters.web} onChange={(event) => update('web', event.target.value)}><option value="">Either</option><option value="true">Required</option><option value="false">Not required</option></select></label><label><span>Authorization</span><select value={filters.auth} onChange={(event) => update('auth', event.target.value)}><option value="">Either</option><option value="true">Required</option><option value="false">Not required</option></select></label><label><span>Code execution</span><select value={filters.code} onChange={(event) => update('code', event.target.value)}><option value="">Either</option><option value="true">Executes code</option><option value="false">No code execution</option></select></label></div></details>
}
